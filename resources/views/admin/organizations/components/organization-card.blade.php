<div class="col">
    <div class="card  ">
        <!-- <div class="card-body"> -->
            {{-- !! comment the link to the question page for now --}}
                {{-- <a href="{{route('organizations.services.show',['service'=>$service->id,'organization'=>$organization->id])}}" class=" btn btn-outline-primary  ">
                    <h4 class="card-title text-center">{{$service->name}}</h4>
                </a> --}}
                {{-- !! end of comment --}}
                <div class=" btn btn-outline-primary  ">
                    <h4 class="card-title text-center">{{$service->name}}</h4>
                </div>
            <!-- </div> -->
        </div>
</div>